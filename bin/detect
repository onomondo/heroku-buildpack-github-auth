#!/bin/sh

# Copied from https://github.com/timshadel/heroku-buildpack-github-netrc/blob/master/bin/detect
# If someone asks for this buildpack, it's got to be in the
# context of another buildpack, like Multipack. Therefore,
# we're best able to communicate the absence of the needed
# environment variable if we fail during the 'compile' task.
echo "github-netrc"
exit 0
